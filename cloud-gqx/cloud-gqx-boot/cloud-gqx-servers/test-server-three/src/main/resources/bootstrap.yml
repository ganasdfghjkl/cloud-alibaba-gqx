spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        refresh-enabled: true
        shared-configs[0]:
          dataId: server-three.yaml
          group: DEFAULT_GROUP
          refresh: true
    sentinel:
      transport:
        dashboard: localhost:8099
  application:
    name: test-server-three
  profiles:
    active: dev
server:
  port: 10004
dubbo:
  registry:
    address: spring-cloud://localhost:8848
  protocol:
    name: dubbo
    port: -1
  cloud:
    subscribed-services: test-server-one
management:
  endpoints:
    web:
      exposure:
        include: '*'